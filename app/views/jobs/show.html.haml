%h1 this is front end developer page
%p= @job.title 
%p= @job.description
%p= @job.company
= button_to "Apply for job" , @job.url
= link_to "Home" , root_path
= link_to "Edit", edit_job_path(@job)
-# = link_to "Delete", job_path(@job), method: :delete, data: {confirm : "are you sure?"}
= link_to "Delete", job_path(@job), method: :delete, data: { confirm: "Are you sure?" }